<html>
   <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Love ♡</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;600;700&family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
</head>
    <style>
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #ffeef8 0%, #f0e6ff 50%, #e6f3ff 100%);
    min-height: 100vh;
    overflow-x: hidden;
}

.container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    padding: 20px;
}

/* Floating hearts animation */
.floating-hearts {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
}

.heart {
    position: absolute;
    color: #ffb6d9;
    font-size: 20px;
    animation: float 6s ease-in-out infinite;
    opacity: 0.7;
}

.heart-1 { left: 10%; animation-delay: 0s; }
.heart-2 { left: 20%; animation-delay: 1s; }
.heart-3 { left: 80%; animation-delay: 2s; }
.heart-4 { left: 70%; animation-delay: 3s; }
.heart-5 { left: 90%; animation-delay: 4s; }

@keyframes float {
    0%, 100% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
    10%, 90% { opacity: 0.7; }
    50% { transform: translateY(-100px) rotate(180deg); }
}

/* Main content */
.main-content {
    text-align: center;
    z-index: 2;
    position: relative;
}

.title {
    font-family: 'Dancing Script', cursive;
    font-size: 3.5rem;
    color: #d63384;
    margin-bottom: 10px;
    text-shadow: 2px 2px 4px rgba(214, 51, 132, 0.2);
    animation: titleGlow 2s ease-in-out infinite alternate;
}

@keyframes titleGlow {
    from { text-shadow: 2px 2px 4px rgba(214, 51, 132, 0.2); }
    to { text-shadow: 2px 2px 20px rgba(214, 51, 132, 0.4); }
}

.subtitle {
    font-size: 1.2rem;
    color: #8e44ad;
    margin-bottom: 40px;
    font-weight: 300;
}

.gift-box {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    border-radius: 25px;
    padding: 40px;
    box-shadow: 0 15px 35px rgba(214, 51, 132, 0.1);
    border: 2px solid rgba(255, 182, 217, 0.3);
}

.gift-icon {
    font-size: 4rem;
    margin-bottom: 20px;
    animation: bounce 2s infinite;
}

@keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-10px); }
    60% { transform: translateY(-5px); }
}

.gift-text {
    font-size: 1.3rem;
    color: #8e44ad;
    margin-bottom: 30px;
    font-weight: 400;
}

/* Magic button */
.magic-button {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
    border: none;
    border-radius: 50px;
    padding: 20px 40px;
    font-size: 1.2rem;
    font-weight: 500;
    color: #8e44ad;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 10px 25px rgba(255, 154, 158, 0.3);
}

.magic-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 35px rgba(255, 154, 158, 0.4);
}

.button-hearts {
    position: absolute;
    top: 50%;
    right: 20px;
    transform: translateY(-50%);
}

.button-hearts span {
    display: inline-block;
    animation: heartPulse 1.5s infinite;
    margin-left: 3px;
}

.button-hearts span:nth-child(2) { animation-delay: 0.3s; }
.button-hearts span:nth-child(3) { animation-delay: 0.6s; }

@keyframes heartPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
}

/* Overlay */
.overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7);
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
}

.overlay.active {
    opacity: 1;
    visibility: visible;
}

/* Popup styles */
.popup {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0.8);
    background: white;
    border-radius: 20px;
    z-index: 1000;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    max-width: 90%;
    max-height: 90%;
    overflow-y: auto;
}

.popup.active {
    opacity: 1;
    visibility: visible;
    transform: translate(-50%, -50%) scale(1);
}

.popup-content {
    padding: 30px;
    position: relative;
}

.close-btn {
    position: absolute;
    top: 15px;
    right: 20px;
    font-size: 2rem;
    cursor: pointer;
    color: #d63384;
    transition: color 0.3s ease;
}

.close-btn:hover {
    color: #8e44ad;
}

/* Flowers popup */
.flowers-popup .popup-content {
    text-align: center;
    background: linear-gradient(135deg, #ffeef8 0%, #f0e6ff 100%);
    border-radius: 20px;
}

.flowers-popup h2 {
    font-family: 'Dancing Script', cursive;
    font-size: 2.5rem;
    color: #d63384;
    margin-bottom: 30px;
}

.bouquet {
    display: flex;
    justify-content: center;
    align-items: flex-end;
    margin: 30px 0;
    flex-wrap: wrap;
    gap: 10px;
}

.flower {
    font-size: 3rem;
    animation: flowerGrow 0.6s ease-out forwards;
    transform: scale(0) translateY(20px);
    display: inline-block;
}

.flower:nth-child(1) { animation-delay: 0.1s; }
.flower:nth-child(2) { animation-delay: 0.2s; }
.flower:nth-child(3) { animation-delay: 0.3s; }
.flower:nth-child(4) { animation-delay: 0.4s; }
.flower:nth-child(5) { animation-delay: 0.5s; }
.flower:nth-child(6) { animation-delay: 0.6s; }
.flower:nth-child(7) { animation-delay: 0.7s; }

@keyframes flowerGrow {
    to {
        transform: scale(1) translateY(0);
    }
}

.flower-message {
    font-size: 1.1rem;
    color: #8e44ad;
    line-height: 1.6;
    font-style: italic;
    margin-top: 20px;
}

/* Letter popup */
.letter-popup .popup-content {
    max-width: 600px;
}

.letter {
    background: #fefefe;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    font-family: 'Dancing Script', cursive;
}

.letter-header h2 {
    color: #d63384;
    font-size: 2rem;
    margin-bottom: 10px;
}

.date {
    color: #8e44ad;
    font-size: 1rem;
    margin-bottom: 25px;
    font-family: 'Poppins', sans-serif;
}

.letter-body p {
    color: #555;
    font-size: 1.2rem;
    line-height: 1.8;
    margin-bottom: 20px;
}

.signature {
    text-align: right;
    margin-top: 30px !important;
    font-size: 1.3rem !important;
    color: #d63384 !important;
}

.name {
    font-size: 1.5rem;
    font-weight: 600;
}

/* Song popup */
.song-popup .popup-content {
    max-width: 500px;
    text-align: center;
    background: linear-gradient(135deg, #e6f3ff 0%, #ffeef8 100%);
    border-radius: 20px;
}

.song-popup h2 {
    font-family: 'Dancing Script', cursive;
    font-size: 2.5rem;
    color: #d63384;
    margin-bottom: 30px;
}

.music-player {
    background: white;
    border-radius: 15px;
    padding: 25px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
}

.album-art {
    font-size: 4rem;
    margin-bottom: 20px;
}

.song-title {
    color: #8e44ad;
    font-size: 1.4rem;
    margin-bottom: 15px;
    font-family: 'Poppins', sans-serif;
}

.song-description {
    color: #666;
    line-height: 1.6;
    margin-bottom: 25px;
    font-style: italic;
}

.player-controls {
    margin: 25px 0;
}

.play-btn {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
    border: none;
    border-radius: 25px;
    padding: 15px 30px;
    font-size: 1.1rem;
    color: #8e44ad;
    cursor: pointer;
    margin-bottom: 20px;
    transition: transform 0.3s ease;
}

.play-btn:hover {
    transform: translateY(-2px);
}

.volume-control {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
}

#volumeSlider {
    width: 150px;
    accent-color: #ff9a9e;
}

.song-note {
    color: #8e44ad;
    font-style: italic;
    margin-top: 20px;
    font-size: 0.95rem;
}

/* Responsive design */
@media (max-width: 768px) {
    .title {
        font-size: 2.5rem;
    }
    
    .subtitle {
        font-size: 1rem;
    }
    
    .gift-box {
        padding: 30px 20px;
    }
    
    .popup-content {
        padding: 20px;
    }
    
    .bouquet .flower {
        font-size: 2rem;
    }
    
    .letter {
        padding: 20px;
    }
    
    .letter-body p {
        font-size: 1.1rem;
    }
}

@media (max-width: 480px) {
    .title {
        font-size: 2rem;
    }
    
    .gift-text {
        font-size: 1.1rem;
    }
    
    .magic-button {
        padding: 15px 25px;
        font-size: 1rem;
    }
}
</style>

<body>
    <div class="container">
        <!-- Background decorations -->
        <div class="floating-hearts">
            <div class="heart heart-1">♡</div>
            <div class="heart heart-2">♡</div>
            <div class="heart heart-3">♡</div>
            <div class="heart heart-4">♡</div>
            <div class="heart heart-5">♡</div>
        </div>
        <!-- Main content -->
        <div class="main-content">
            <h1 class="title">For My Beautiful Girlfriend</h1>
            <p class="subtitle">Even miles apart, you're always in my heart</p>
            
            <div class="gift-box">
                <div class="gift-icon">💝</div>
                <p class="gift-text">I have something special for you...</p>
                <button class="magic-button" id="surpriseBtn">
                    <span class="button-text">Open Your Valentine's Surprise</span>
                    <div class="button-hearts">
                        <span>♡</span><span>♡</span><span>♡</span>
                    </div>
                </button>
            </div>
        </div>
        <!-- Popup overlays -->
        <div class="overlay" id="overlay"></div>
        
        <!-- Flowers popup -->
        <div class="popup flowers-popup" id="flowersPopup">
            <div class="popup-content">
                <span class="close-btn" data-popup="flowers">&times;</span>
                <h2>A Bouquet Just For You 🌸</h2>
                <div class="bouquet">
                    <div class="flower flower-1">🌹</div>
                    <div class="flower flower-2">🌸</div>
                    <div class="flower flower-3">🌺</div>
                    <div class="flower flower-4">🌷</div>
                    <div class="flower flower-5">🌹</div>
                    <div class="flower flower-6">🌸</div>
                    <div class="flower flower-7">🌺</div>
                </div>
                <p class="flower-message">These flowers represent my love that blooms for you every day, no matter the distance between us. Each petal carries a kiss from me to you. 💕</p>
            </div>
        </div>
        <!-- Letter popup -->
        <div class="popup letter-popup" id="letterPopup">
            <div class="popup-content">
                <span class="close-btn" data-popup="letter">&times;</span>
                <div class="letter">
                    <div class="letter-header">
                        <h2>My Dearest Love,</h2>
                        <div class="date">Valentine's Day 2024</div>
                    </div>
                    <div class="letter-body">
                        <p>Every sunrise reminds me of the warmth you bring to my life, and every sunset makes me grateful for another day of loving you.</p>
                        
                        <p>Though we may be separated by miles, nothing can diminish the love I feel for you. You are my first thought in the morning and my last thought at night.</p>
                        
                        <p>Your laugh is my favorite melody, your smile is my favorite view, and your love is my favorite feeling in the entire world.</p>
                        
                        <p>I promise that every day until we're together again, I'll love you more than the day before. You are my today and all of my tomorrows.</p>
                        
                        <p class="signature">Forever yours,<br>
                        <span class="name">Your Love</span> ♡</p>
                    </div>
                </div>
            </div>
        </div>
        <!-- Song popup -->
        <div class="popup song-popup" id="songPopup">
            <div class="popup-content">
                <span class="close-btn" data-popup="song">&times;</span>
                <h2>Our Special Song 🎵</h2>
                <div class="music-player">
                    <div class="album-art">🎼</div>
                    <div class="song-info">
                        <h3 class="song-title">"Intertwine" - Over October Feat. The Ridleys</h3>
                        <p class="song-description">This song perfectly captures how I feel about you - I love all of you, every curve and every edge, all your perfect imperfections. 💕</p>
                        
                        <div class="player-controls">
                            <button class="play-btn" id="playBtn">▶️ Play Our Song</button>
                            <div class="volume-control">
                                <span>🔊</span>
                                <input type="range" id="volumeSlider" min="0" max="100" value="50">
                            </div>
                        </div>
                        
                        <!-- You can replace this with an actual audio file -->
                        <audio id="audioPlayer" preload="auto">
                             <source src="your-song.mp3" type="audio/mpeg"> 
                        </audio>
                        
                        <p class="song-note">💫 This will always be our song, playing in my heart whenever I think of you.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script>
document.addEventListener('DOMContentLoaded', function() {
    const surpriseBtn = document.getElementById('surpriseBtn');
    const overlay = document.getElementById('overlay');
    const popups = {
        flowers: document.getElementById('flowersPopup'),
        letter: document.getElementById('letterPopup'),
        song: document.getElementById('songPopup')
    };
    
    let currentPopupIndex = 0;
    const popupOrder = ['flowers', 'letter', 'song'];
    
    // Main surprise button click
    surpriseBtn.addEventListener('click', function() {
        showNextPopup();
    });
    
    // Function to show the next popup in sequence
    function showNextPopup() {
        if (currentPopupIndex < popupOrder.length) {
            const popupToShow = popupOrder[currentPopupIndex];
            showPopup(popupToShow);
            currentPopupIndex++;
        }
    }
    
    // Function to show a specific popup
    function showPopup(popupName) {
        overlay.classList.add('active');
        popups[popupName].classList.add('active');
        
        // Add entrance animation
        setTimeout(() => {
            popups[popupName].style.transform = 'translate(-50%, -50%) scale(1)';
        }, 50);
    }
    
    // Function to hide all popups
    function hideAllPopups() {
        overlay.classList.remove('active');
        Object.values(popups).forEach(popup => {
            popup.classList.remove('active');
        });
    }
    
    // Close button event listeners
    document.querySelectorAll('.close-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            const popupType = this.getAttribute('data-popup');
            hidePopup(popupType);
            
            // Show next popup after a delay
            setTimeout(() => {
                showNextPopup();
            }, 500);
        });
    });
    
    // Function to hide specific popup
    function hidePopup(popupName) {
        popups[popupName].classList.remove('active');
        
        // If it's the last popup, hide overlay
        if (currentPopupIndex >= popupOrder.length) {
            setTimeout(() => {
                overlay.classList.remove('active');
                // Reset for replay
                currentPopupIndex = 0;
            }, 300);
        }
    }
    
    // Overlay click to close
    overlay.addEventListener('click', function() {
        hideAllPopups();
        currentPopupIndex = 0;
    });
    
    // Prevent popup content clicks from closing the popup
    Object.values(popups).forEach(popup => {
        popup.addEventListener('click', function(e) {
            e.stopPropagation();
        });
    });
    
    // Music player functionality
    const playBtn = document.getElementById('playBtn');
    const audioPlayer = document.getElementById('audioPlayer');
    const volumeSlider = document.getElementById('volumeSlider');
    
    let isPlaying = false;
    
    playBtn.addEventListener('click', function() {
        if (!isPlaying) {
            // Since we don't have an actual audio file, we'll simulate
            this.innerHTML = '⏸️ Pause';
            this.style.background = 'linear-gradient(135deg, #98fb98 0%, #87ceeb 100%)';
            isPlaying = true;
            
            // You can uncomment this when you add an actual audio file
            // audioPlayer.play();
        } else {
            this.innerHTML = '▶️ Play Our Song';
            this.style.background = 'linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%)';
            isPlaying = false;
            
            // audioPlayer.pause();
        }
    });
    
    // Volume control
    volumeSlider.addEventListener('input', function() {
        const volume = this.value / 100;
        // audioPlayer.volume = volume;
        
        // Visual feedback
        this.style.background = `linear-gradient(to right, #ff9a9e 0%, #ff9a9e ${this.value}%, #ddd ${this.value}%, #ddd 100%)`;
    });
    
    // Initialize volume slider appearance
    volumeSlider.style.background = `linear-gradient(to right, #ff9a9e 0%, #ff9a9e 50%, #ddd 50%, #ddd 100%)`;
    
    // Add some sparkle effects
    function createSparkle() {
        const sparkle = document.createElement('div');
        sparkle.innerHTML = '✨';
        sparkle.style.position = 'fixed';
        sparkle.style.left = Math.random() * window.innerWidth + 'px';
        sparkle.style.top = Math.random() * window.innerHeight + 'px';
        sparkle.style.pointerEvents = 'none';
        sparkle.style.fontSize = '20px';
        sparkle.style.zIndex = '10';
        sparkle.style.animation = 'sparkleFloat 3s ease-out forwards';
        
        document.body.appendChild(sparkle);
        
        setTimeout(() => {
            sparkle.remove();
        }, 3000);
    }
    
    // Add sparkle animation to CSS
    const style = document.createElement('style');
    style.textContent = `
        @keyframes sparkleFloat {
            0% {
                opacity: 1;
                transform: translateY(0px) rotate(0deg);
            }
            100% {
                opacity: 0;
                transform: translateY(-100px) rotate(360deg);
            }
        }
    `;
    document.head.appendChild(style);
    
    // Create sparkles periodically
    setInterval(createSparkle, 2000);
});

    </script>
</body>
    </html>
